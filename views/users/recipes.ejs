<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/styles.css">
    <title>Recipes - CookBook</title>
</head>
<body>
    <div class="container px-4 px-lg-5">
    <%
    if(recipes.length > 0){
        for(let i=0; i < recipes.length; i++){
        %>

            <div class="row gx-4 gx-lg-5 align-items-center my-5">
                <div class="col-lg-7"><img src="" alt="image" class="img-fluid rounded mb-4 mb-lg-0"></div>
                <div class="col-lg-5">
                    <h1 class="font-weight-light"><%= recipes[i].title %></h1>
                    <p><%= recipes[i].description %></p>
                    <p><%= recipes[i].ingredients %></p>
                    <a href="#" class="btn btn-primary">Call to action</a>
                </div>
            </div>
        </div>
        <%
        }
    }
    else{
        %>
        <h1>Recipes not available</h1>
        <%
    }
    %>

</body>
</html>